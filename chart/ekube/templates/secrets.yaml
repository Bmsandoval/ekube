apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "ekube.fullname" . }}-secrets
  labels:
    app: {{ template "ekube.fullname" . }}
    release: "{{ .Release.Name }}"
data:
  secrets.json: |
    {{ .Values.Secrets | toJson | indent 4 }}
